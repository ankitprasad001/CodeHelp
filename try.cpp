#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cout<<"Enter n:- ";
    cin >> n;
    vector<int>fa;
    vector<int>fb;

    int arr[n];

cout<<"Enter Element\n";
    for (int i = 0; i < n; i++)
    {
        cin >> arr[n];
    }

    int q;
    cout<<"Enter q:- ";
    cin >> q;
    int a,b,c;
    while(q--){
        cout<<"Enter a b c\n";
        cin>>a>>b>>c;
    }
    return 0;
}
